const gameContainer = document.querySelector('.game-container');
const abortGameBtn = document.querySelector('.btn abort-game-btn');
const userNameEl = document.querySelector('.user-name-el');
const navigationMenuEl = document.querySelector('.navigation-menu');

export {
  gameContainer,
  abortGameBtn,
  userNameEl,
  navigationMenuEl,
}
